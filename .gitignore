# ---- Secrets (NEVER commit these) ----
.env
.env.local

# NOTE: js/config.js is intentionally COMMITTED for the static GitHub Pages
# deploy (the browser needs the key). Treat that Groq key as public and rotate
# it if abused. Do NOT put private keys (e.g. NVIDIA) in config.js.

# ---- Dependencies / build ----
node_modules/
dist/
build/

# ---- OS / editor junk ----
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
